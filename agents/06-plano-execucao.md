# 06 — Plano de Execução e Log de Iterações

## Como o agente trabalha

O backlog vivo é o **GitHub Issues** (uma issue por fase, com checklist dentro). Este documento espelha as fases e guarda o log de iterações. O protocolo completo está no `README.md` desta pasta; resumo:

1. Abrir a **issue aberta mais antiga** (ignorando as de label `evolucao`).
2. Reler `01`, `04`, `05` + o `AGENTS.md` das pastas tocadas (e a seção de migrations do `02` se a issue criar tabelas).
3. Implementar pelos templates do `05`.
4. Validar: `go build ./... && go vet ./... && go test ./...` (+ `-race` se invariante disputada).
5. Fechar a issue e registrar a linha no **log de iterações** abaixo — ao fechar a **fase**, anexar a conferência do grafo de pacotes no **Dependency Graph do Go Architect** (doc `01`).

Uma issue por vez. Nunca pular fase. Nunca deixar o build quebrado.

## Fases

| Fase | Tema | Issue | Definição de pronto |
|---|---|---|---|
| **F0** | Fundação | [#1](https://github.com/gothout/workspace-api/issues/1) | CLI cobra (`serve`/`migrate`/`seed`), `pkg/config`, `rest_err` (com registro global de erros), `pagination`, `orgctx`, `validator`, postgres (Connect puro + singleton fatal), runner de migrations com CLI completa + `up` automático no boot (advisory lock) + teste `up → down → up`, `GET /api/status`, Swagger no ar, `arch-go.yml` + teste com coverage 100 — build/vet/test verdes. |
| **F1** | Autenticação e autorização granular | [#2](https://github.com/gothout/workspace-api/issues/2) | `infra/jwt` (access+refresh), cadeia `SetContextAuthorization → ResolveWorkspace → RequirePermission` fail-closed no `internal/middleware` (sem importar `domain`), resolução por Host com white-label + `X-Workspace-Id` como fallback, permissão `dominio:subdominio:acao` declarada rota a rota, migrations de `identidade_user_papel`/`identidade_user_papel_permissao` (**globais da plataforma, sem coluna de escopo** — exceção documentada) e `identidade_user_atribuicao` (com `organization_uuid` + `workspace_uuid`), seed dos 5 papéis com as permissões granulares, resolvedor de permissões do middleware como **adaptador no `cmd/bootstrap`** consultando essas tabelas (provisório documentado: na F4 passa a delegar ao service do user). |
| **F2** | Subdomínio organization | [#3](https://github.com/gothout/workspace-api/issues/3) | Os 8 arquivos do subdomínio + `permissions.go` + **pacote `model/` do domínio** (`internal/identidade/model/organization`) com entidades/VOs + migration up/down testada + rotas + campo `dominio` custom (opcional, único global, validado) + seed de organization raiz; checklist do `05` completo. |
| **F3** | Subdomínio workspace | [#4](https://github.com/gothout/workspace-api/issues/4) | Idem F2, com slug único global + reservados, resolução por Host validando o pertencimento à organization dona do domínio, e acesso de suporte auditado; checklist do `05` completo. |
| **F4** | Subdomínio user + login | [#5](https://github.com/gothout/workspace-api/issues/5) | CRUD de user (escopo por organization), atribuição de papéis por workspace, tabela `identidade_user_refresh_token` (revogação persistida por `jti`), credencial fechada no subdomínio com comparação de senha exposta **por interface**; aplicação `internal/identidade/application/auth` com login/refresh/logout **sem distinguir "não existe" de "senha errada"**, rodando só com a resolução da organization pelo Host; resolvedor de permissões do middleware passa a **delegar ao service do user**; checklist do `05` completo. |
| **F5** | Catálogos do sistema | [#6](https://github.com/gothout/workspace-api/issues/6) | `internal/identidade/application/catalogo` agregando os registros no bootstrap; `GET /api/application/identidade/catalogo/permissoes/minhas` (árvore filtrada do usuário) e `GET /api/system/errors` (mapa completo) no ar, conforme os contratos do `04`. |
| **F6** | Hardening do template | [#7](https://github.com/gothout/workspace-api/issues/7) | Testes de concorrência onde houver invariante (slug, e-mail, unicidade), cobertura Swagger conferida por teste, arch-go com coverage 100, grafo de dependências validado com o Go Architect (registro no log abaixo), README de uso do template. |

## Evoluções futuras (label `evolucao`)

| Tema | Issue | Resumo (desenho no doc 02) |
|---|---|---|
| Redis | [#8](https://github.com/gothout/workspace-api/issues/8) | Cache de permissões e de workspace por slug, locks, denylist de JWT — cliente degradável, ligado por interface. **Entrou no loop pós-revisão (story E1).** |
| ClickHouse + logs assíncronos | [#9](https://github.com/gothout/workspace-api/issues/9) | Auditoria/acesso/erro fora do caminho síncrono, writer em lote, stdout como destino degradado. **Entrou no loop pós-revisão (story E2).** |
| errobserve | [#10](https://github.com/gothout/workspace-api/issues/10) | Observador de erros por subdomínio com sinks plugáveis, alimentado pelo catálogo de erros. **Entrou no loop pós-revisão (story E4).** |

## Revisão pós-F6 (achados da revisão de código)

Correções priorizadas após a revisão técnica do template fechado (R1 e R2 são críticos; R3–R6 importantes; R7 varredura menor). Rate limiting/login e cache do caminho quente ficam na evolução Redis (#8).

| Story | Tema | Issue | Definição de pronto |
|---|---|---|---|
| **R1** | Escalação de privilégio em CriarApiKey | [#19](https://github.com/gothout/workspace-api/issues/19) | CriarApiKey valida cada permissão pedida contra as efetivas do ctx (`middleware.Atende`); `*:*` só se o criador a possui; teste com `admin_organization` tentando `*:*`. |
| **R2** | lock_timeout de migrations | [#20](https://github.com/gothout/workspace-api/issues/20) | Sessão de migration em conexão dedicada (`sql.Conn`) ou DSN própria — pool do gorm nunca sai com `lock_timeout` alterado; `up→down→up` real. |
| **R3** | Provisionamento inicial | [#21](https://github.com/gothout/workspace-api/issues/21) | Seed opcional (flag/env, nunca automático no boot) cria super_admin + workspace inicial, idempotente, documentado no README; teste de integração. |
| **R4** | Cascata de inativação da organization | [#22](https://github.com/gothout/workspace-api/issues/22) | Inativar organization revoga refresh tokens dos usuários e API keys dela; `Refresh` e `BuscarApiKeyPorHash` checam status da dona; testes de integração. |
| **R5** | Logout idempotente + rotação de refresh | [#23](https://github.com/gothout/workspace-api/issues/23) | Logout com token já revogado não falha; refresh revoga o jti anterior; reuso de refresh revogado falha fechado; testes. |
| **R6** | Segredo JWT mínimo 32 bytes | [#24](https://github.com/gothout/workspace-api/issues/24) | `infra/jwt` exige 32+ bytes com erro claro; `configs_example.json` e testes ajustados. |
| **R7** | Varredura menor | [#25](https://github.com/gothout/workspace-api/issues/25) | `errors.Is` nos adaptadores; race em `middleware.Use`/`ResetarParaTeste`; `trusted_proxy` aplicado; código morto removido; e-mail mascarado em logs; regex `slugdns` deduplicada; `rest_err.DoCatalogo` determinístico; doc `ResolvedorWorkspaces` alinhada — cada item corrigido ou justificado; `-race` verde. |

## Evoluções no loop (pós-revisão)

| Story | Tema | Issue | Definição de pronto |
|---|---|---|---|
| **E1** | Redis — cache, locks, denylist e rate-limit de login | [#8](https://github.com/gothout/workspace-api/issues/8) | `internal/infra/redis` degradável; denylist do JWT em Redis ligada no bootstrap; cache de slug do workspace e de permissões efetivas com TTL + invalidação; rate-limit/lockout de login (429 padronizado, degradável); prefixos de chave documentados; `docker-compose.yml` de dev + `configs_example.json`; testes de degradação/cache/lockout com Redis efêmero; `-race` verde. |
| **E2** | ClickHouse + trilhas de log assíncronas | [#9](https://github.com/gothout/workspace-api/issues/9) | `internal/infra/clickhouse` degradável com writer em lote (flush por tamanho/intervalo, descarte contado, drain no shutdown); `internal/pkg/log/{audit_log,access_log}` folha com interface `Destino` ligada no bootstrap; log nunca síncrono; middleware global de access log com `ray_trace`; trilha de auditoria das escritas migrada; DDL versionada + `configs_example.json`; stdout degradado; testes de buffer/flush/drain/degradação; `-race` verde. |
| **E3** | Catálogo de eventos de auditoria | [#26](https://github.com/gothout/workspace-api/issues/26) | `events.go` por subdomínio que audita (ação estável + descrição PT-BR + campos do payload), mesmo padrão do `errors.go`/`permissions.go`; `auditar()` valida a catalogação (evento novo não catalogado reprova); **teste de cobertura de eventos nos dois sentidos** (ação usada sem estar catalogada / entrada catalogada sem uso / subdomínio que audita sem `events.go` no lugar certo); bootstrap agrega; `GET /api/system/eventos` expõe o mapa completo; Swagger regenerado. |
| **E4** | errobserve — eventos de erro + namespace reservado | [#10](https://github.com/gothout/workspace-api/issues/10) | `internal/pkg/errobserve` (folha) com `Observe(ctx, err)` devolvendo o erro intacto; sentinela fora do catálogo vira `ErrUnknown` (critical); severidades warn/error/critical; **namespace reservado `sistema.*`** para eventos de plataforma (boot, migrations, shutdown, degradação) — subdomínio de negócio não pode registrar nele; sinks plugáveis (slog, ClickHouse da E2, `[ALERTA]` para critical com janela); telemetria nunca bloqueia nem muda a resposta; `-race` verde. |
| **E5** | Endpoints de leitura de logs | [#27](https://github.com/gothout/workspace-api/issues/27) | Leitura por trilha (`logs/auditoria`, `logs/acesso`, `logs/erros`); **escopo em 3 recortes**: super_admin lê qualquer organization (filtro opcional), organization lê a própria (alheio = 403/404), workspace lê o recorte do próprio workspace; filtros (workspace, ação, usuário, janela, ray_trace); paginação + ordem desc; permissão `identidade:logs:ler` rota a rota (seed atualizado, super_admin via `*:*`); ClickHouse ausente = 503 padronizado; contrato do doc 04 alinhado ao catálogo de eventos; mappings atualizados; `-race` verde. |

## Log de iterações

Uma linha por issue fechada. Formato: data ISO, issue (número real), arquivos criados/alterados, resultado do build/testes, observações úteis para a próxima iteração.

| data | issue | arquivos criados/alterados | resultado build/testes | observações |
|---|---|---|---|---|
| 2026-08-22 | #1 (F0) | `main.go`, `configs_example.json`, `arch-go.yml`, `arquitetura_test.go`; `internal/pkg/{config,rest_err,pagination,orgctx,validator}`; `internal/infra/database/postgres`, `internal/infra/database/migrations`, `internal/infra/jwt`; `cmd/cli`, `cmd/bootstrap`, `cmd/server` + `cmd/server/routes` | build/vet/test verdes; arch-go compliance 100 + coverage 100; smoke test end-to-end (`serve` com Postgres efêmero: `/api/status` ok, Swagger 200, 404 padronizado, SIGTERM drena) | `-race` indisponível localmente (sem gcc/CGO) — rodar na CI; migrations runner ignora arquivos `-- manual`; `migrate validate/create` rodam sem conexão |
| 2026-08-22 | #2 (F1) | `db/migrations/000{1,2}_identidade_user_*.{up,down}.sql`; `internal/infra/jwt/{token,revogacao}.go`; `internal/pkg/orgctx` (getter `Permissoes`); **`internal/middleware/{contratos,cadeia,autorizacao,resolucao,log}.go`**; `cmd/bootstrap/{middleware,seed}.go` + ligação em `bootstrap.go`; CORS com domínios custom em `routes.go` | build/vet/test verdes (incl. `up→down→up` real das novas migrations e integração de seed/resolvedor com Postgres efêmero); arch-go compliance 100 + coverage 100; smoke boot com middleware ligado | resolvedores provisórios consultam as tabelas de autorização direto (`cmd/bootstrap/middleware.go`) — na F3/F4 passam a delegar aos subdomínios, contratos do middleware NÃO mudam; provedor de domínios custom e validador de X-Api-Key entram na F2 (até lá: fail-closed); `-race` segue para a CI; conferência visual pendente até o fim da fase no Dependency Graph do Go Architect |
| 2026-08-22 | #3 (F2) | `db/migrations/000{3,4}_identidade_organization_*.{up,down}.sql`; `internal/identidade/model/organization` (entidades Organization+ApiKey, VO DominioCustom/ParseDominio com PSL real via golang.org/x/net, chaves SHA-256); `internal/identidade/domain/organization` (8 arquivos + permissions.go + contratos.go da cascata); `cmd/bootstrap/organizacao.go` (suspensor provisório F3, provedor de domínios custom e ResolvedorApiKeys delegando ao singleton na chamada); seed da organization raiz; rotas registradas em `routes.go` | build/vet/test verdes (`up→down→up` real das migrations; integração ponta a ponta com Postgres efêmero cobrindo unicidade TOTAL do domínio, cascata degradada, ciclo de apikey e adaptadores do middleware); arch-go compliance 100 + coverage 100; smoke: boot aplica 4 migrations, `[BOOTSTRAP-DI] Contêiner Identidade/Organization`, rotas 401 fail-closed, Swagger 200, seed idempotente | VO do domínio custom chama-se `DominioCustom` (colisão com a constante de catálogo `Dominio`); projeção do provedor usa `uuid AS organization_uuid` (raiz é dona dela mesma) e filtra `deleted_at IS NULL` à mão (Table() cru não herda soft delete); `-race` segue para a CI |
| 2026-08-22 | #4 (F3) | `db/migrations/0005_identidade_workspace_create.{up,down}.sql`; `internal/identidade/model/workspace` (entidade Workspace, VO Slug/ParseSlug DNS 3-63, StatusWorkspace, sentinelas de invariante); `internal/identidade/domain/workspace` (8 arquivos + permissions.go + contratos.go com CacheResolucao por interface — nil é normal); `cmd/bootstrap/workspace.go` (resolvedor BuscarPorSlug/PorUUID e suspensor da cascata delegando ao singleton na chamada), `middleware.go` enxugado (SQL direto saiu), bootstrap liga `[BOOTSTRAP-DI] Contêiner Identidade/Workspace`; 6 rotas em `/api/domain/identidade/workspaces` rota a rota; docs/ regenerado (`swag init`) | build/vet/test verdes (`up→down→up` real da migration; integração ponta a ponta: unicidade global do slug, slug removido NÃO se libera, isolamento por organization, cascata suspende só os filhos dela, resolução por slug com cache fake); arch-go compliance 100 + coverage 100 | slug único TOTAL no banco (exceção documentada ao índice parcial); slugs fixos (`www`, `api`, `app`, `admin`, `docs`, `status`, `mail`, `suporte`, `painel`) moram no service do subdomínio e o middleware pergunta a ele; suporte auditado (`[SUPORTE]`) no resolvedor de permissões do bootstrap; Swagger esquecido no commit da fase — regenerado na revisão (rodar `swag init` antes de commitar rota nova); `-race` segue para a CI |
| 2026-08-22 | #5 (F4) | `db/migrations/0006_identidade_user_create.{up,down}.sql` (`identidade_user_user` + `identidade_user_refresh_token`); `internal/identidade/model/user` (User com `senha_hash` `json:"-"`, RefreshToken, Atribuicao, Papel/PapelPermissao globais, VO Email/ParseEmail, ValidarSenha 8–72); `internal/identidade/domain/user` (8 arquivos + permissions.go + contratos.go com ValidadorWorkspaces; credencial bcrypt DENTRO do subdomínio via `Credenciais`/`NovasCredenciaisBcrypt()`; login INDISTINGUÍVEL com hash de mentira em `sync.Once`; revogação persistida por jti; suporte `[SUPORTE]` movido para cá); `internal/identidade/application/auth` (7 arquivos sem model/repository: contratos Usuarios/EmissorToken/ResolvedorOrganization, login/refresh/logout, host sem organization = organization aleatória → MESMO caminho/corpo); `cmd/bootstrap/usuario.go` (resolvedorPermissoesUser DELEGANDO ao service — provisório F1 saiu —, revogadorRefresh, validadorWorkspaces, adaptadores do auth), middleware.go enxugado; bootstrap liga `[BOOTSTRAP-DI] Identidade/User` + `Identidade/Auth` + `DefinirRevogador`; 8 rotas em `/api/domain/identidade/users` + 3 em `/api/application/identidade/auth/*`; docs/ regenerado | build/vet/test verdes (unit: indistinguibilidade com spy de comparações, escopo, atribuição, sessões; integração REAL com postgres efêmero: resolvedor via service sobre esquema migrado+semeado, login/sessão/revogação com bcrypt real, inativação revoga sessões, `up→down→up` da 0006); arch-go compliance 100 + coverage 100; smoke ponta a ponta: boot 4 contêineres, login/refresh/logout/401 genérico idêntico nas 3 falhas, cadeia 401/403/200, `[SUPORTE]` auditado, PATCH inativar, 409 e-mail duplicado | rate-limit/lockout seguem evolução futura (sem rotação de refresh também — documentado no service); resolução do login por Host NÃO cobre base_domain nu/painel/api (console master é caso especial futuro); FKs de `atribuicao` continuam por uuid (precedente F3); e-mail único é índice PARCIAL por organization (libera após remoção — diferente de slug/domínio); `-race` segue para a CI |
| 2026-08-22 | #6 (F5) | `internal/identidade/application/catalogo` (contratos.go com ProvedorPermissoes + tipos únicos do agregado, dto_response.go com as duas árvores do doc 04, service, controller com Routes + RoutesSistema, singleton, errors.go sem catálogo próprio — documentado); `internal/middleware` exporta `Atende` (matcher único do RequirePermission); `cmd/bootstrap/catalogo.go` (agregador dos Catalogo() dos 3 subdomínios → forma única) + ligação `[BOOTSTRAP-DI] Contêiner Identidade/Catalogo`; `cmd/server/routes` registra a aplicação no grupo /api/application E `/api/system/errors` direto no engine; seed ganha `identidade:catalogo:ler` nos 4 papéis humanos; agents/03 (tabela de papéis + rota) e middleware AGENTS.md atualizados; docs/ regenerado | build/vet/test verdes; arch-go compliance 100 + coverage 100; smoke ponta a ponta: super_admin recebe árvore inteira, usuario_workspace vê só as 2 ações :ler dele, `/api/system/errors` pública devolve os 4 grupos (auth 3 / organization 13 / user 17 / workspace 8), 401 sem token, Swagger serve os dois paths | matcher de permissão vive SÓ no middleware (`Atende`) — filtragem da árvore usa o MESMO casamento do RequirePermission; `/api/system/errors` é montada NO ENGINE (grupo mudaria o path do contrato); catalogo não registra catálogo no rest_err (catálogo vazio emitiria grupo vazio no mapa); `-race` segue para a CI |
| 2026-08-22 | #7 (F6) | `cmd/bootstrap/concorrencia_test.go` (disputa de slug movida para cá e de e-mail nova, com barreira de disparo + `aquecerPool` — pool aberto antes da largada; e-mail também provado livre entre organizations diferentes); `cmd/bootstrap/swagger_cobertura_test.go` (engine REAL montado com os 5 singletons sobre o efêmero × `docs/swagger.json`, rotas→docs e docs→rotas, wildcards gin normalizados para OpenAPI); `grafo_dependencias_test.go` na raiz (grafo via `go list -json ./...` com classificação por camada e GATE das arestas proibidas do doc 01: infra↔infra, middleware→domain/application, model→acima de pkg, domain→domain/application/cmd, application→domain/cmd); README.md reescrito (o que é, stack, como subir com migrations automáticas, CLI, como criar subdomínio novo passo a passo, como o front consome os catálogos, deploy ops); AGENTS.md revisados (middleware: nota do resolvedor provisório F1→F4 atualizada; bootstrap: lista de arquivos-frente corrigida) | build/vet/test verdes; **`go test -race ./...` VERDE localmente** (gcc instalado no ambiente — CGO habilitado; slug+e-mail disputados por 12 goroutines com pool aquecido, zero data race); arch-go compliance 100 + coverage 100; cobertura Swagger verde nos dois sentidos (18 paths × 23 rotas de negócio) | Dependency Graph do Go Architect é app GUI e o módulo não compila no toolchain atual (x/tools antigo, tokeninternal) — a conferência virou GATE executável equivalente (`grafo_dependencias_test.go`), que imprime as arestas por par de camadas e reprova qualquer uma fora do fluxo `pkg ← infra ← model ← domain ← application ← cmd`; grafo conferido: application→{model,pkg}, domain→{middleware,model,pkg}, infra→pkg, middleware→{infra,pkg}, model→pkg, cmd→todas — ZERO arestas proibidas; swagger coverage pula sem docker como os demais testes de integração |
| 2026-08-22 | #19 (R1) | `internal/identidade/domain/organization/service.go` (`exigirPermissoesPossuidas`: cada permissão pedida casada contra as efetivas do ctx via `middleware.Atende`, antes da persistência; forma global `*:*` conferida por pertencimento exato — 2 segmentos ficam fora da gramática do Atende); `errors.go` (sentinela `ErrPermissaoNaoPossuida`, code `identidade.organization.permissao_nao_possuida`, status 403); `controller.go` (traduzir + descrição Swagger da 403); `service_test.go` (suíte `TestCriarApiKeyNaoEscalaPrivilegio` table-driven: admin_organization tentando `*:*`, ação fora do papel, ctx vazio fail-closed, curinga atendido, super_admin concede `*:*`); `cmd/bootstrap/organizacao_test.go` (recusa provada sobre o esquema real + ctxs ajustados às permissões efetivas); AGENTS.md do subdomínio atualizado | build/vet/test verdes (integração testcontainers incluída); `-race` verde no pacote tocado; Swagger regenerado e cobertura conferida | A ordem importa: validação de FORMATO continua no construtor do model (400), posse é AUTORIZAÇÃO (403) e roda depois — testes antigos de entrada inválida não mudam de código; pedida com curinga exige o `*` NO EFETIVO daquele segmento (nunca o contrário), então Atende é são nessa direção; ctx sem permissões injetadas recusa toda criação de chave (fail-closed) — dublês/integrações precisam injetar efetivas |
| 2026-08-23 | #20 (R2) | `internal/infra/database/migrations/migrations.go` (`FonteConexao` ganha `SessaoDedicada()`; `novaInstancia` roda sobre pool exclusivo de 1 conexão e aplica os timeouts NESSA conexão via `aplicarTimeouts` — valores em ms puros, formato de duração do Go não é sintaxe Postgres; `encerrar` fecha a sessão ao fim de cada operação); `internal/infra/database/postgres/postgres.go` (`AbrirPoolSQL` puro, database/sql sem gorm, blank import do stdlib pgx); `cmd/bootstrap/bootstrap.go` (`fonteBanco.SessaoDedicada` com MaxOpenConns=1); AGENTS.md do runner atualizado; testes: fakes com sessão dedicada real, `TestTimeoutsFicamNaConexaoDedicada`, `TestPoolDeNegocioNaoContaminadoPelaMigracao` (duas conexões do pool seguradas juntas, settings antes/depois idênticos), `TestOperacoesFalhamSemSessaoDedicada` | build/vet/test verdes (22 pacotes, integração testcontainers incluída); `-race` verde nos pacotes tocados; arch-go compliance 100 + coverage 100; `up→down→up` real revalidado pelo teste de migrations | o driver pgx/v5 do golang-migrate v4.19 já roda TUDO em `*sql.Conn` própria — o bug era só o SET no pool errado, que além de não valer para o DDL contaminava a conexão devolvida; `SET statement_timeout = '10m0s'` é inválido no Postgres (formato Go) — usar inteiro em ms (`pg_settings.setting` é determinístico para assert, SHOW normaliza unidade); `Migrate.Close()` fecha conn E db do driver ⇒ quem cria a instância deve dar defer de encerrar |
| 2026-08-23 | #21 (R3) | `cmd/bootstrap/seed.go` (provisionamento OPCIONAL: `Provisionamento`+`NovoProvisionamento` exige e-mail+senha juntos, slug padrão `principal`; fluxo workspace → super_admin → atribuição TODOS pelos services dos subdomínios com construtores PUROS (nunca singleton: sync.Once é do serve), entradas validadas cedo pelos VOs antes de tocar o banco; idempotência lookup-first (ResolverPorSlug/BuscarPorEmail/Atribuicoes); slug alheio NUNCA tomado; adaptador `validadorWorkspacesSeed` liga o contrato do user ao repositório puro do irmão); `cmd/cli/cli.go` (flags `--super-admin-email/--super-admin-senha/--workspace-slug` + envs `WORKSPACE_API_SEED_*`); README.md (seção "Provisionamento inicial" com flags/env/login ponta a ponta); agents/02 (Seeds) e cmd/cli AGENTS.md atualizados; testes novos em `cmd/bootstrap/provisionamento_test.go` (validação pré-banco sem docker, integração idempotente com bcrypt+login+vínculo reais, recusa de slug reservado/alheio abortando sem criar usuário) e `cmd/cli/cli_test.go` (recusa incompleta antes da config, flags montadas) | build/vet/test verdes (22 pacotes, testcontainers reais); `-race` verde nos pacotes tocados; arch-go compliance 100 + coverage 100 (sem pacote novo — tudo em cmd/bootstrap e cmd/cli) | zero SQL de negócio novo no cmd: até o uuid determinístico da raiz virou helper compartilhado (`uuidOrganizacaoRaiz`) entre seed e provisionamento; falha de formato morre nos VOs ANTES do banco (testes unitários passam db nil); validador do user no seed não pode resolver singleton inexistente — mesma regra do boot, outra fonte (repo puro escopado) |
| 2026-08-23 | #22 (R4) | `internal/identidade/domain/organization` (`contratos.go` novo contrato `EncerradorSessoesUsuarios`; `repository.go` `RevogarApiKeysDaOrganization` escopada + `BuscarApiKeyPorHash` com JOIN na dona ativa — defesa em profundidade além da cascata; `service.go` cascata unificada `executarCascata` ANTES da persistência: workspaces → chaves → sessões, auditoria com contagens; `singleton.go` `New(db, suspensore, encerrador)`); `internal/identidade/domain/user` (`RevogarTokensAtivosDaOrganization` escopada + service `RevogarSessoesDaOrganization`, idempotentes); `internal/identidade/application/auth` (`contratos.go` `VitalidadeOrganization`; `sessaoDoToken` fail-closed com dona inativa/removida E com peça faltando; singleton exige 4 contratos); `cmd/bootstrap/{usuario.go,bootstrap.go}` (adaptadores `vitalidadeOrganizacao` e `encerradorSessoesUsuario` resolvendo NA CHAMADA); testes: unitários nos 3 pacotes + integração REAL `TestCascataDeInativacaoEncerraSessoesEApiKeys` (login JWT real → inativação → sessões mortas no banco, refresh/logout falham fechado, X-Api-Key de dona inativa recusada pelo JOIN); ponta a ponta e Swagger cobertura booted user singleton antes da organization | build/vet/test verdes (22 pacotes, testcontainers reais); `-race` verde em identidade/...; integração docker PASS | ordem da cascata é fail-closed: qualquer passo falhando impede a persistência do novo estado (pai nunca fica inativo com acesso vivo; direção segura no pior caso); Reativar NÃO desfaz a cascata (acessos voltam só por ação explícita); logout bloqueado pela vitalidade NÃO revoga — quem revoga é a cascata no momento da inativação (dublê do auth precisa emular isso, senão "reativação ressuscita" falso-negativo); testes que bootam singletons no bootstrap agora precisam do user ANTES de exercitar cascata da organization (MustUse panicaria) |
| 2026-08-23 | #23 (R5) | `internal/infra/jwt/token.go` (`ValidarAssinatura`: assinatura/método/expiração/tipo SEM consultar o revogador — porta EXCLUSIVA do logout idempotente; `Validar` refatorado sobre `parseValido` comum, denylist só nele); `internal/identidade/application/auth` (`contratos.go` `EmissorToken.ValidarSemRevogacao`; `service.go` logout via NOVA `claimsParaEncerrar` — não passa mais pela porta do refresh: não exige jti ativo, conta autenticável nem dona viva; Refresh COM ROTAÇÃO: `EncerrarSessao` do jti anterior ANTES de emitir o par novo — direção fail-closed, emissão falha depois da revogação mata a sessão); `cmd/bootstrap/usuario.go` (adaptador `emissorToken.ValidarSemRevogacao`) + `cascata_organization_test.go` ajustado (logout de token rodado pela rotação/cascata é sucesso); testes: unitários `TestRefreshRotacionaERevogaOJtiAnterior`, `TestLogoutRepetidoNaoFalha`, R4 atualizado + integração REAL nova `cmd/bootstrap/auth_rotacao_test.go` (`TestLogoutIdempotentePontaAPonta`, `TestRefreshRotacionaPontaAPonta` — JWT HMAC verdadeiro sobre Postgres efêmero, revogação provada no banco por `RefreshTokenRevogado`); docs agents/{03,jwt,user,auth AGENTS.md} + descrição Swagger do logout | build/vet/test verdes (22 pacotes); `-race` verde em auth+jwt+bootstrap (integração incluída); Swagger regenerado e conferido | logout é DESTRUIÇÃO: nunca concede acesso, então não precisa das portas fail-closed do refresh (vitalidade/conta/jti ativo) — idempotência exige ler claims SEM a denylist; rotação revoga ANTES da emissão: pior caso é o dono re-logar, nunca dois refresh válidos coexistindo; linha ausente no banco continua recusando logout (anomalia de infra honesta) |
| 2026-08-23 | #24 (R6) | `internal/infra/jwt/jwt.go` (`comprimentoMinimoSegredo` 16 → 32; erro claro citando bytes e o motivo — HS256 exige chave de 256 bits); `internal/infra/jwt/AGENTS.md` (regra do mínimo documentada); `configs_example.json` + `agents/02` (exemplo novo de 45 bytes, ainda placeholder reconhecível); `internal/pkg/config/config.go` (guarda "valor de exemplo em produção" atualizada para o MESMO literal novo — acoplamento que quebraria a validação silenciosamente); README.md (mínimo de 32 bytes na seção de setup); testes: `jwt_test.go` (segredos < 32 substituídos + `TestConnectExigeSegredoDe32Bytes`: 31 recusa com "32 bytes" no erro, exatamente 32 passa), `middleware/doubles_test.go` e `infra/postgres/helpers_test.go` (fixtures de config com segredo ≥ 32), `config_test.go` (teste do segredo-exemplo usa o literal novo) | build/vet/test verdes (22 pacotes, integração incluída); `-race` verde em jwt+config+middleware | configs.json local (gitignored) atualizado junto para o boot local não morrer; a guarda do valor-exemplo em config.go é ACOPLADA ao literal do configs_example.json — mudar um sem o outro desativa a proteção de produção em silêncio |
| 2026-08-23 | #25 (R7) | Varredura menor — 8 itens: `cmd/bootstrap/usuario.go` (`errors.Is` nos 2 pontos com `==`); `internal/middleware/cadeia.go` (mutex RWMutex cobrindo Use/MustUse/New/ResetarParaTeste — o lock cobre o once.Do INTEIRO, pois o reset escreve em `once`) + `cadeia_concorrencia_test.go` NOVO (24 goroutines × Use/New-falho/Resetar, roda sob -race); `cmd/server/routes` (`Opcoes.TrustedProxies` + `engine.SetTrustedProxies` no Montar; Montar devolve erro de config → boot fatal; lista vazia = nenhum proxy confiável); `cmd/server/http_server.go` (`ServirAteSinal` removido + imports órfãos); `internal/pkg/pii` NOVO (`MascaraEmail`: inicial do local + domínio; lixo vira "***") aplicado ao login-falho do auth e criar/remover do user; regex `slugdns` com fonte ÚNICA no validator (`model/workspace.ParseSlug` delega em `SlugValido`; pkg é folha alcançável por todos); `rest_err.DoCatalogo` determinístico (candidatos ordenados por grupo+código, teste com 50 execuções e empate em 3 catálogos); doc do `ResolvedorWorkspaces` alinhada ao comportamento real (inativo volta Ativo=false e a CADEIA responde 404) | build/vet/test verdes (23 pacotes, integração incluída); `-race` verde em middleware/rest_err/pii/server/routes/model-workspace/bootstrap; arch-go 100/100 pelo gate do go test | o -race pegou corrida REAL que eu mesmo introduzi: proteger instance sem proteger `once` não basta quando ResetarParaTeste recria o Once — lock tem que envolver o Do inteiro; Montar mudando de assinatura ripple para routes_test e swagger_cobertura_test (require.NoError após cada chamada); teste de empate do DoCatalogo precisa códigos ÚNICOS entre catálogos (RegistrarCatalogo panicaria em duplicado) |
| 2026-08-23 | #8 (E1) | `internal/infra/redis` NOVO (`redis.go` Connect puro NUNCA-erra + singleton degradável com mutex cobrindo o once.Do; `chaves.go` prefixos fixos `perm:`, `workspace:slug:`, `lock:c|b:{hash}`, `idempot:` reservado, `jwt:deny:` + builders com normalização e hash sha256 do par e-mail/IP — PII não repousa crua; `denylist.go` Denylist que implementa ESTRUTURALMENTE `jwt.RevogadorDeRefresh` sem importar jwt, só POSITIVO cacheado; `lockout.go` LimitadorLogin contador+janela+marcador de bloqueio) + AGENTS.md do pacote com tabela de chaves/donos/TTLs; `internal/pkg/config` (`databases.redis{enabled,host,port,pass,db}` + `cache{ttl_resolucao_seg,ttl_permissoes_seg,login_lockout{max_tentativas,janela_seg,bloqueio_seg}}` com defaults em validar()); `internal/identidade/domain/user` (`contratos.go`: ObservadorAtribuicoes + OpcaoServico/ComObservadorAtribuicoes variadic — chamadores existentes não mudam; service dispara o observador DEPOIS de AtribuirPapel (par exato) e RemoverAtribuicao (uuid.Nil = grosseira); singleton repassa opções); `internal/identidade/application/auth` (`contratos.go`: LimitadorLogin opcional + ErrLoginBloqueado; errors.go code `identidade.auth.login_bloqueado` status 429; service checa ANTES do caminho de autenticação, registra falha/sucesso, falha do limitador NUNCA recusa login; LoginEntrada.IP via c.ClientIP(); controller anota 429); `cmd/bootstrap/cache_redis.go` NOVO (`revogadorComCache` composto denylist+fonte com fonte injetável; `cacheResolucaoRedis` implementando CacheResolucao com InvalidarOrganization seletiva por SCAN; `novoResolvedorPermissoesComCache(base)` decorando o delegado do user com cache perm:*; `invalidadorPermissoesRedis` observador com DEL exato ou varredura; `limitadorLoginAuth` adaptador da config corrente); bootstrap.go (passo 4.5 Redis degradável + Close LIFO, DefinirRevogador(novoRevogadorComCache(nil)), workspace recebe cacheResolucaoRedis{}, user recebe ComObservadorAtribuicoes, auth recebe Limite); middleware.go usa o decorador; docker-compose.yml NOVO (postgres+redis dev); configs_example.json + configs.json local + README (seção "Redis opcional") + agents/{02,03} + AGENTS.md de infra, jwt, auth, workspace, user, bootstrap; testes: redis pkg unitário (3 estados do Connect, chaves fixas, no-op degradado) + integração efêmera (denylist positivo/TTL, lockout janela/teto/bloqueio/cumprimento, ciclo único do singleton), bootstrap `cache_redis_test.go` (composição com fonte/base contadoras, invalidação exata e grosseira, fiação REAL atribuição→observador→Redis, lockout bloqueia-cumpre-libera, degradação em todos), auth 4 testes unitários novos | build/vet/test verdes (25 pacotes, integração postgres+redis reais); `-race` verde nos pacotes tocados; arch-go segue 100/100 pelo gate do go test (redis classificado como infra pela regra existente); Swagger regenerado (429 documentado no login) | tags mapstructure têm que casar COM AS CHAVES DO JSON EXEMPLO (`bloqueio_seg`, não BloqueioSec) — mismatch vira default silencioso e o teste de TTL pegou (marcador vivia 900s); conformidade estrutural resolve infra→infra sem import: a interface continua sendo do consumidor (jwt) e a implementação mora no outro pacote; negativo nunca cacheado elimina invalidação na denylist inteira; variadic OpcaoServico evita ripple de assinatura em seed/testes; worktree foi resetado por processo externo no MEIO da iteração (commits E3/E4 de planejamento entraram) — trabalho não commitado sobreviveu por backup em /tmp/opencode: commitar cedo e manter cópia fora do worktree |
| 2026-08-23 | #9 (E2) | `internal/pkg/log/{audit_log,access_log}` NOVOS (pacotes-folha: `Evento` + interface `Destino` + `SlogPadrao` stdout degradado determinístico; `audit_log.Detalhes` normaliza os pares k→v montados à mão); `internal/infra/clickhouse` NOVO (`clickhouse.go` Connect puro NUNCA-erra [DEGRADADO] + singleton com mutex cobrindo o once.Do; `escritor.go` writer assíncrono em lote — filas limitadas por trilha, flush por tamanho OU janela, fila cheia DESCARTA E CONTA via atomic, falha de gravação perde lote COM contagem, `Fechar()` drena filas+parciais com timeout; `gravador.go` PrepareBatch/Send nativo sobre as tabelas do DDL); `db/logs/000{1,2}_*.sql` NOVOS (MergeTree particionado por mês, ORDER BY (instante, ray_trace), idempotentes) + AGENTS.md da pasta; `internal/pkg/config` (`databases.clickhouse` degradável + `logs{lote_tamanho,lote_janela_ms,fila_tamanho,drain_timeout_sec}` com defaults em validar()); `cmd/bootstrap/logs.go` NOVO (`iniciarTrilhas`: escritor quando existe / SlogPadrao quando não; adaptadores finos `destinoAuditoria`/`destinoAcesso` resolvendo a sobrecarga impossível dos dois Destino.Registrar); `routes.go` (middleware access log PRIMEIRO da cadeia substituindo o slog síncrono; ray_trace do ctx caindo ao header X-Request-Id; Opcoes.AcessoLog); auditar() dos 4 services migrados para a trilha (organization/workspace ganharam OpcaoServico variadic + ComTrilha; user reusou o padrão da E1; auth ganhou Deps.Trilha) — fallback slog legado preservado para montagem direta em teste; bootstrap.go passo 4.6 + ComTrilha nos 3 subdomínios + auth + AcessoLog no engine + Close LIFO drenando; docker-compose.yml clickhouse dev; configs_example.json seção logs+clickhouse; README seção ClickHouse; agents/02 atualizada (ClickHouse ✅); AGENTS.md de infra, pkg/log, clickhouse, bootstrap, routes; testes: pkg/log unitários, escritor (flush tamanho/janela/fila cheia/drain/idempotência/falha contada), Connect degradado, ciclo único do singleton, integração testcontainers aplicando o DDL real, routes com destino capturando ray_trace, bootstrap trilhas_log_test.go | build/vet/test verdes (27 pacotes); `-race` verde em clickhouse/pkg-log/routes/domínios/auth/bootstrap; arch-go segue 100/100 pelo gate do go test; smoke ponta a ponta REAL: postgres+clickhouse efêmeros, boot ligado, login/criar-organization por HTTP e as DUAS trilhas consultáveis no workspace_logs após o SIGTERM (detalhes JSON ordenados, PII mascarada preservada, ray_trace correlacionando request↔auditoria) | Go não sobrecarrega métodos: duas interfaces Destino com o mesmo nome de método exigem adaptador fino por trilha no único lugar que pode conhecê-los (bootstrap/logs.go); worker que só descarrega por tamanho/janela NUNCA entra na gravação antes do lote completar — teste de fila cheia precisa de LoteTamanho=1 para sincronizar pelo pegouLote sem esperar o ticker; protocolo nativo do ClickHouse não aceita multi-statement nem comentário fora de lugar: aplicar DDL = strip de comentários ANTES do split por ';'; Ping do Connect valida EXISTÊNCIA do database — aplicar o DDL exige conexão bootstrap ao database default primeiro |
| 2026-08-23 | #26 (E3) | `internal/identidade/domain/{organization,workspace,user}/events.go` + `internal/identidade/application/auth/events.go` NOVOS (`EventoMeta` nativo + `catalogoEventos` com ação estável + descrição PT-BR + `Campos` do payload; `CatalogoEventos()` exportado; `validarAcaoCatalogada` PANICA em ação fora do catálogo — chamada na 1ª linha de cada `auditar()`); `events_test.go` nos 4 pacotes (ação catalogada passa, fantasma panica com mensagem acionável); `internal/identidade/application/catalogo` (`contratos.go`: ProvedorEventos + EventoMeta forma única COM Dominio/Subdominio preenchidos pelo bootstrap; `dto_response.go`: EventosResponseDto/GrupoEventosDto/EventoDto com NovoEventosResponseDto agrupando e ordenando por (dominio, subdominio, acao), campos nil → lista vazia; `service.go`: MapaDeEventos; `singleton.go` exige Eventos; `controller.go`: RotaEventosSistema `/api/system/eventos` registrada no engine pelo RoutesSistema, pública como a de erros); `cmd/bootstrap/catalogo.go` (novoAgregadorEventos convertendo os 4 catálogos nativos + copiarCampos isolando as fatias); bootstrap.go Dependencias.Eventos; swagger_cobertura_test ajustado; **`eventos_cobertura_test.go` NOVO** — varredura AST (go/parser) dos fontes dos subdomínios: reprova (a) ação emitida via `auditar(...)`/par `"acao", "x"` sem entrada no events.go, (c) entrada catalogada sem emissão, (b) pacote sob identidade/{domain,application} que emite sem events.go/CatalogoEventos — mutações provaram as 3 direções; TestAgregadorEventosConverteCatalogosNativos (contagem por grupo preservada); docs/* regenerados (swag init); agents/04 (rotas de sistema + seção Auditoria + CONTRATO — mapa de eventos), AGENTS.md de catalogo/bootstrap/domain atualizados | build/vet/test verdes (28 pacotes, integração real incluída); `-race` verde em catalogo/auth/domínios/bootstrap; cobertura Swagger verde com `/api/system/eventos`; arch-go segue no gate do go test (nenhum pacote novo) | o catálogo cobre TODO o vocabulário auditado do subdomínio: além das ações do auditar(), os eventos slog com chave "acao" (cascata_organization_inativada nos 3, suporte_concedido no user) entraram no events.go e são varridos pela AST — logs de recusa SEM chave "acao" (ex.: apikey_permissao_recusada) ficam fora do catálogo por decisão documentada; tipos nativos homônimos exigem conversão explícita no agregador (mesma regra dos PermissaoMeta); teste de cobertura mora no bootstrap porque precisa ler os fontes relativos à raiz E importar os 4 CatalogoEventos |
| 2026-08-23 | #10 (E4) | `internal/pkg/errobserve` NOVO (folha: `Observador` imutável com `Observe(ctx, err)` devolvendo o erro INTACTO; `For` registra no singleton, `Novo` construtor puro de validação; `DoCatalogo(errorCatalog, severidades)` deriva as entradas DO PRÓPRIO catálogo do errors.go — fonte única dos códigos; sentinela fora do catálogo vira evento `desconhecido` + critical; namespace RESERVADO `sistema.*` — negócio não registra nele (pânico no For/Novo), `ObservadorPlataforma` exige o prefixo e o vocabulário fixo `CatalogoSistema()` (boot/migrations.up/shutdown/degradação) fica visível sem emissão; registro global determinístico + `DefinirSinks` mantendo SlogPadrao SEMPRE primeiro; despachante recupera pânico POR sink com contagem `FalhasSink()`; sink `Alerta(janela)` agrega críticos por código na janela `[ALERTA]`); decoradores `service_observado.go` nos 3 subdomínios + auth (NewService devolve o service DECORADO — singleton/seed/testes observam pelo mesmo caminho) com severidades declaradas em `severidadesErros` no `singleton.go` (warn=4xx esperado, error=escalação R1/lockout 429/hash ausente 500, critical=desconhecido); clickhouse terceira trilha (`filaErros`+`EnfileirarErros`+drain+descarte contado, `InserirErros`, tabela `log_erro`) + `db/logs/0003_log_erros.sql`; bootstrap (`logs.go` define sinks slog+alerta+CH e observa degradação do CH; `errobserve.go` NOVO observador de plataforma — migrations falha = sistema.migrations.up critical via ErrMigracao, redis/CH degradado = sistema.degradacao_dependencia warn via ErrDegradacao; `catalogo.go` agrega códigos de erro + sistema/plataforma em /api/system/eventos; `mapa_erros.go` NOVO para a CLI); CLI `workspace-api errors` (mapa global código/severidade/status/mensagem — sem conexão nem config); config `logs.alerta_janela_seg` default 60s; eventos_cobertura_test atualizado (agregado = auditoria nativa + erros observados + plataforma); `errobserve_cobertura_test.go` NOVO (decoração ponta a ponta com repo dublê: catalogada/desconhecida/sucesso-sem-evento/catálogo global); docs agents/{02,04} + AGENTS.md raiz/README + AGENTS.md de errobserve/clickhouse/db-logs/bootstrap/cli/domain/auth | build/vet/test verdes (29 pacotes, integração postgres+redis+clickhouse reais); `-race` verde em errobserve/clickhouse/config/rest_err/domínios/auth/bootstrap/cli; arch-go segue 100/100 pelo gate (pkg→pkg ok); Swagger regenerado sem diff (nenhuma rota nova) | ResetarParaTeste apaga o registro populado pelos vars de pacote na importação — teste que precisa do catálogo global deve trocar só os SINKS (DefinirSinks sem extras restaura) e nunca resetar; errors.Is NÃO casa string concatenada — wrapping de plataforma usa fmt.Errorf %w sempre; contrato do sink inclui segurança para concorrência (o -race pegou o dublê do teste antes do código real); severidade é declaração do singleton, código/mensagem vêm do errorCatalog — sentinela nova sem severidade PANICA no import, fail-fast antes de qualquer boot |
| 2026-08-23 | #27 (E5) | `db/logs/0004_log_acesso_tenancy.sql` NOVO (ALTER TABLE idempotente: organization_uuid/workspace_uuid/user_uuid na trilha de acesso — pré-requisito do recorte); `internal/pkg/log/access_log` (Evento com tenancy; stdout imprime quando presente); `cmd/server/routes` (middleware access log preenche tenancy DEPOIS da cadeia; uuid.Nil → vazio); `internal/infra/clickhouse` (`consultor.go` NOVO: Consultor+FiltroTrilha lendo as 3 trilhas com filtros, ordem instante DESC/ray_trace DESC e count à parte; INSERTs com lista EXPLÍCITA de colunas; singleton ganha UseConsultor()); `internal/identidade/application/logs` NOVO (contratos.go ConsultaTrilhas sobre os tipos das folhas pkg/log+errobserve; service com recorte em 3 níveis derivado do ctx — *:* por pertencimento exato = plataforma, Atende(ler_organization) = org inteira, resto = par (org,ws) resolvido; filtro fora do recorte = ErrForaDoEscopo 404; dto_request com validação própria de uuid/RFC3339/janela; permissions.go PermLer+PermLerOrganization; errors.go codes identidade.logs.* com 503 padronizado; observador errobserve com severidades; controller rota a rota GET logs/{auditoria,acesso,erros}); `cmd/bootstrap/logs.go` ganha consultorLogs resolvendo NA CHAMADA (degradado = ErrIndisponivel → 503); bootstrap boota [BOOTSTRAP-DI] Contêiner Identidade/Logs; routes registra identidade.logs; seed: identidade:logs:ler em admin_organization/admin_workspace/somente_leitura e :ler_organization só no admin_organization; catalogo.go agrega o Catalogo() da aplicação; swagger_cobertura_test boota a app com dublê; `logs_leitura_test.go` NOVO integração real postgres+clickhouse (escrita→flush→leitura pelos 3 recortes, filtros ray_trace/código/janela, degradação 503); testes unitários da aplicação (recortes/fail-closed/filtros/paginação/tradução) e do controller (rota a rota + cadeia não inicializada fecha 403); docs agents/04 (CONTRATO leitura de logs), AGENTS.md raiz/pacotes/db-logs, README | build/vet/test verdes (30 pacotes, integrações reais incluídas); CGO_ENABLED=1 go test -race verde em clickhouse/pkg-log/routes/logs/bootstrap; arch-go compliance 100 + coverage 100 pelo gate do go test; Swagger regenerado (3 paths novos) e cobertura verde nos dois sentidos | a trilha de ACESSO não tinha colunas de escopo — sem o DDL 0004 o recorte seria impossível (vazamento entre tenants); count() do ClickHouse é UInt64 (scan direto em int64 reprova) e status é Int32 (idem para int); bind do driver v2.40.3 falhou de forma bizarra com a fatia de args construída INLINE no chamada variadic — montar a fatia numa variável (argumentosConsulta) estabilizou; permissão de ROTA é só identidade:logs:ler (recorte workspace) e a AMPLIAÇÃO para organization é permissão SEPARADA conferida no service — RequirePermission continua single-string |
| 2026-08-23 | #28 (UX1) | `internal/identidade/domain/user` — `repository.go` (`ListarPapeis` no `RepositorioAtribuicoes`: consulta GLOBAL ordenada por nome, mesma exceção documentada do `PapelPorUUID`; lista vazia vira fatia vazia, nunca null); `service.go` (`Service.Papeis`, delegação pura — leitura: sem auditoria e sem escopo) + `service_observado.go` (override observando erros); `dto_response.go` (`PapelResponseDto` uuid/nome/descricao + `NovosPapeisResponseDto`); `permissions.go` (`PermAtribuirPapel` ganha o par rota+método GET `/api/domain/identidade/user/papeis` no `Catalogo()` — quem atribui papel é quem precisa do Select; seed já cobre via curingas, zero mudança de dados); `controller.go` (`PrefixoRotasPapeis`, rota registrada FORA do grupo /users com cadeia completa rota a rota, handler com Swagger); testes: `papeis_test.go` NOVO unitário (catálogo global com ctx cru, vazio = lista vazia, rota exposta no Catalogo), `cmd/bootstrap/papeis_leitura_test.go` NOVO integração REAL postgres efêmero (`TestListarPapeisSobreEsquemaReal`: 5 papéis seed com uuid+descrição e ordem determinística via construtores puros; `TestSeedCobreRotaDePapeis`: concessão da rota pelos curingas conferida com `middleware.Atende` nos 5 papéis); AGENTS.md do subdomínio atualizado; docs/ regenerado (swag init) | build/vet/test verdes (30 pacotes, integrações reais incluídas); CGO_ENABLED=1 go test -race verde em user+bootstrap; arch-go segue 100/100 pelo gate do go test; cobertura Swagger verde com o path novo | path do contrato é `/api/domain/identidade/user/papeis` (issue #28/PRD), FORA da árvore `/users/{uuid}` — evita colisão com o parâmetro e casa com o pedido do painel; permissão REUSADA (atribuir_papel) em vez de criar `listar_papeis`: catálogo menor, seed intacto (curinga `identidade:user:*` dos admins já atende — provado pelo teste com Atende); leitura não audita nem entra no events.go |
| 2026-08-23 | #29 (UX2) | `internal/identidade/application/logs` — `contratos.go` (contrato `ResolvedorUsuarios` + `UsuarioLog` + `Dependencias.Usuarios` opcional: join entre bases não existe, resolução EM LOTE por uuids distintos da página); `service.go` (`OpcaoServico`/`ComResolvedorUsuarios` variadic — chamadores existentes intactos; `coletarUserUUIDs` genérico + `usuariosDasLinhas`: falha do resolvedor NUNCA derruba a consulta, log warn e campos vazios); `dto_response.go` (`user_nome`/`user_email` nos 3 DTOs; builders recebem o mapa resolvido); `singleton.go` liga a opção quando presente; `internal/identidade/domain/user/repository.go` (`BuscarPorUUIDs` em lote: ESCOPADA quando o ctx carrega organization, GLOBAL no caminho da plataforma — exceção documentada; ausentes simplesmente faltam); `cmd/bootstrap/logs.go` (adaptador `resolvedorUsuariosLogs` resolvendo MustUse NA CHAMADA, função de repo injetável para testes) + ligação em bootstrap.go; AGENTS.md da aplicação atualizado; testes: unitários novos em logs (`usuarios_logs_test.go`: enriquecimento nas 3 trilhas, lote distinto, sem-usuário não resolve, degradação) + integração REAL postgres efêmero (`cmd/bootstrap/logs_usuarios_test.go`: escopo do resolvedor contra organization alheia, plataforma atravessa, ponta a ponta com fantasma vazio); docs/ regenerado | build/vet/test verdes (30 pacotes, integrações reais incluídas); CGO_ENABLED=1 go test -race verde em logs+user+bootstrap; arch-go segue 100/100 pelo gate do go test; Swagger regenerado (3 schemas com user_nome/user_email) | trilha no ClickHouse × usuário no Postgres = join impossível: contrato de resolução EM LOTE declarado no CONSUMIDOR e ligado no bootstrap (mesma disciplina dos demais contratos); recorte do ctx vale para o resolvedor também — org alheia não é resolvida mesmo com uuid na mão; enriquecimento é leitura COMPLEMENTAR: falha degrada para campos vazios com log, nunca derruba a resposta |
| 2026-08-23 | #30 (UX3) | `internal/identidade/application/logs` — `contratos.go` (`ProvedorOpcoes` + `OpcaoFiltro` uuid+nome, EXIGIDO no boot; ponteiro nil = sem filtro na dimensão = caminho plataforma); `service.go` (`OpcoesFiltro` com os TRÊS recortes derivados do ctx — plataforma lista tudo, organization a própria inteira, workspace usuários COM ATRIBUIÇÃO nele; fail-closed = ErrForaDoEscopo; Metodo/ClasseStatus ZERADOS em auditoria/erros — colunas só existem no acesso); `dto_request.go` (query `metodo` normalizado e `status_classe` 2xx–5xx → faixa [n00,(n+1)00)); `dto_response.go` (OpcoesFiltroResponseDto, listas vazias = []); `controller.go` (GET /opcoes-filtro rota a rota, PermLer basta) + permissions.go (par novo nas duas permissões de leitura); `internal/infra/clickhouse/consultor.go` (whereComum aplica metodo/classe_status quando presentes); repositórios dos 3 subdomínios ganham projeções uuid+nome (`ListarOpcoes`: organization nil=TODAS exceção raiz / workspace por org / user por org OU atribuição viva com deleted_at explícito); `cmd/bootstrap/logs.go` (adaptador `provedorOpcoesLogs` sobre repos puros resolvendo NA CHAMADA, funções injetáveis); swagger_cobertura_test com dublê do contrato novo; AGENTS.md da aplicação + agents/04 (CONTRATO opções + filtros novos); testes: unitários (recortes com ponteiros capturados, fail-closed, parsing/propagação metodo-classe, whereComum), integração REAL postgres (`TestOpcoesFiltroSobreEsquemaReal`: três recortes sobre esquema migrado+semeado) e clickhouse efêmero (`TestFiltrosMetodoEClasseNaTrilhaDeAcesso`: drain determinístico + filtros combináveis); docs/ regenerado | build/vet/test verdes (30 pacotes, integrações reais incluídas); CGO_ENABLED=1 go test -race verde em logs/clickhouse/domínios/config/bootstrap; arch-go segue 100/100 pelo gate; Swagger regenerado com o path novo | config.Init é sync.Once: SEGUNDO Init no mesmo processo é no-op silencioso — teste que grava config própria precisa config.ResetarParaTeste ANTES (helper configParaLogsLeitura agora resetta e o defer não vaza container morto); sintomas pareciam flake de infra ("connection refused" no porto mapeado do CH antigo); quem lê logs NÃO precisa dos :ler dos irmãos para montar Selects — as opções saem recortadas pelo MESMO recorte da leitura; workspace vence org na granularidade das opções de usuário (atribuição viva decide) |
| 2026-08-24 | #32 (UX4) | `internal/identidade/model/workspace` (`CreateInput.OrganizationPedida` opcional + `ListFilter.OrganizationUUID` `form:"-"` — exceção da gestão cross-tenant documentada nos comentários); `internal/identidade/domain/workspace` — `contratos.go` NOVO contrato `ResolvedorEstadoOrganization` (existe/ativa do irmão organization, ligado no bootstrap); `service.go` (`ehPlataforma` por PERTENCIMENTO EXATO de `*:*` — mesma exceção de R1/E5; `resolverOrganizacaoAlvo`: não-plataforma alheia = ErrForaDoEscopo 404 sem vazar existência, plataforma confere existência/vitalidade e REESCOPEIA o ctx na ALVO antes de persistir; `List` aplica o filtro só para plataforma trocando o escopo — sem filtro, comportamento intacto inclusive o fail-closed; opção variadic ComEstadoOrganizacao — nil = criação cross-tenant RECUSADA); `errors.go` (+3 sentinelas catalogadas: fora_do_escopo 404 / organization_nao_encontrada 404 / organization_inativa 422); `singleton.go` (severidades: fora_do_escopo = error, sinal de segurança como no recorte dos logs); `controller.go` (parse manual de organization_uuid na query — inválido = 400; traduzir com as sentinelas novas; Swagger anotado); `dto_request.go` (organization_uuid opcional no corpo com binding uuid); `events.go` ("criar" ganha campo cross_tenant); `cmd/bootstrap/workspace.go` (adaptador estadoOrganizacaoAlvo resolvendo MustUse NA CHAMADA) + bootstrap.go liga a opção; AGENTS.md do subdomínio reescrito para a exceção UX4; testes: unitários (tabela dos 5 casos de criação, filtro nos recortes plataforma/org/alheia, unicidade global preservada, fail-closed do dublê Criar emulado), integração REAL postgres `cmd/bootstrap/cross_tenant_test.go` (primeiro workspace de org nova pela plataforma, org inexistente/inativa recusadas SEM persistir, admin_organization alheia = fora_do_escopo, própria aceita, listagem pelos 3 recortes, auditoria com cross_tenant=true, Read escopado intacto); docs/ regenerado (swag init) | build/vet/test verdes (29 pacotes, integrações reais incluídas); CGO_ENABLED=1 go test -race verde em workspace+bootstrap+routes; arch-go segue 100/100 pelo gate do go test; Swagger regenerado com os params/respostas novos | GORM ignora Where em INSERT: o fail-closed real do Create vem do ERRO ACUMULADO quando não há escopo — por isso a criação cross-tenant reescopeia o ctx na organização ALVO depois da autorização (super_admin no console master, sem org no ctx, cria sem abrir exceção nenhuma); dublê de repo precisa EMULAR isso (Criar recusa ctx sem org) senão o teste valida um comportamento que não existe; sem restrição de "só o primeiro workspace" — seria inaplicável sob concorrência sem transação serializável, e slug único global + auditoria cobrem o risco |
| 2026-08-24 | #33 (UX5) | `internal/identidade/application/provisionamento` NOVO (aplicação que orquestra organization → workspace → user: contratos Organizacoes/Workspaces/Usuarios/Papeis; service com ordem fail-closed — VOs cedo, alvo existe+ativa, já-provisionada=409, workspace PELO subdomínio via UX4, ctx REESCOPEIADO na alvo para o admin, admin pré-existente reconhecido, duplicata de atribuição = idempotência interna ErrAtribuicaoExistente; events.go com ação provisionar e e-mail SEMPRE mascarado via pkg/pii; errors.go 8 sentinelas catalogadas identidade.provisionamento.*; permissions.go PermExecutar rota a rota; singleton Dependencias obrigatórias + severidades; controller com Swagger); `cmd/bootstrap/provisionamento_app.go` NOVO (adaptadores resolvendo MustUse NA CHAMADA e TRADUZindo o vocabulário dos irmãos — aplicação nunca importa domain); bootstrap.go liga [BOOTSTRAP-DI] Contêiner Identidade/Provisionamento; routes.go registra na árvore /identidade/organizations; catalogo.go agrega permissão+evento; eventos_cobertura_test ganha o 5º emissor conhecido; swagger_cobertura_test boota a aplicação nova; AGENTS.md de application (+regra de sentinelas não-recatalogadas), cmd/bootstrap, agents/03 (seção Provisionamento); testes: unitários (feliz com auditoria mascarada, entradas inválidas antes do banco, alvo/idempotência table-driven, admin reconhecido, duplicata engolida, papel ausente) + integração REAL postgres `provisionamento_endpoint_test.go` (bcrypt+login reais, papel atribuído no banco, 409 na repetição, recusas sem criar nada, slug global tomado, recuperação de tentativa interrompida); docs/ regenerado | build/vet/test verdes (30 pacotes, integrações reais incluídas); CGO_ENABLED=1 go test -race ./... VERDE completo; arch-go segue 100/100 pelo gate do go test; Swagger regenerado com o path novo | sentinelas de irmão NUNCA são recatalogadas (code duplicado pânica no init do rest_err — pegou no primeiro run): o adaptador TRADUZ para o vocabulário da aplicação; toda entrada de catálogo exige severidade no singleton (pânico errobserve no import); seed dos papéis é PRÉ-REQUISITO do provisionamento (PorNome sem seed = ErrPapelAusente mapeado 500); FK real derruba truque de "org inexistente para tomar slug" — teste usa org real |
